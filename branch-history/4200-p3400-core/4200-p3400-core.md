---
id: 4200-p3400-core
subject: '[clang][contracts] add the contract-facet framework'
depends: []
regenerates: []
fixes: []
---

## Rationale
P3400, "Controlling Contract-Assertion Properties": the label framework --
`pre<L>(...)` -- and the machinery that turns a label into answers about
the contract it is attached to.  The separable facets follow in their own
commits; this is what they all need.

The design is that a label is **an ordinary C++ object**, not a syntax.
`pre<my_label>(...)` names something whose type answers questions by having
members, and the compiler asks those questions by constant-evaluating calls
on it.  That is why the framework is as large as it is:
`evaluateFacetConstant`, `facetCallViable`, `callLabelMethod` and
`applyLabelFacets` are a small overload-resolution-and-constant-evaluation
engine, run at compile time against a user-written type.

Three things worth a reviewer's attention:

**`warnNearMissFacet` exists because the failure mode is silence.**  A
label that *almost* provides a facet -- right name, wrong signature or
wrong constness -- simply does not provide it, and the contract gets the
default.  Nothing is ill-formed and nothing is diagnosed, so a typo in a
label's member costs you the behaviour you asked for with no indication.
The near-miss warning is the compensation, and
`dropInaccessibleFacetCandidates` is what keeps it from firing on members
that are private for good reason.

**`contract_control` is a keyword, and the reason is the using-directive.**
`using contract_control namespace N;` makes `N`'s labels findable from
contracts without making them findable from ordinary code, which needs its
own lookup list on `Scope` (that storage is in
`1070-p2900-base-sema-predicate-scope`) and its own parse path here.  It is
introduced through `CONTRACTS_P3400_KEYWORD` in `TokenKinds.def`, gated on
the flag like the other per-paper keywords.

**Most of `<contracts>`'s growth is label algebra.**  The 439 added lines
here are `empty_label`, `review`, `fixed_message_label`, and above all
`operator|` -- combining two labels into one that answers each question
from whichever operand answers it.  That is library, not compiler, and it
is where a reader should start: the combination rules make the compiler
side much easier to follow.

`populateContractSemanticState` is where the whole thing lands -- the
resolved answers are written into the `ContractStmt`'s cached state, the
same slots `2400-p3595` fills from configuration.  Where both apply, that
is the interaction to look at.

## Compile gap
Needs `1020-p2900-base-ast` for the label slot on `ContractStmt`,
`1040-p2900-base-parse` for the cached `<...>` tokens -- that commit caches
them without parsing them precisely so this one can -- `1060-p2900-base-sema-core`,
`1070-p2900-base-sema-predicate-scope` for the contract-control lookup
storage, and `2400-p3595` for the semantic state it writes into.  All
precede it.

**Inward, six later commits reach into `applyLabelFacets`**: each facet in
`4230`-`4280` adds its question to the set this framework asks.  They are
not stubbed here -- the framework asks only about the facets it knows, and
each later commit adds one.

**Behaviour trap:** a label naming a facet this commit does not yet
implement is not an error.  It is a label whose facet is never consulted,
exactly like the near-miss case above, and with the same absence of a
diagnostic.  Between here and `4280-p3400-facet-queryable`, each
unimplemented facet behaves that way -- which is the expected intermediate
state, but is worth knowing before reading a test that "passes" at the
wrong commit.

## Contents
- clang/include/clang/Basic/BuiltinTraits.td : *
- clang/include/clang/Basic/DiagnosticGroups.td : #0:1
- clang/include/clang/Basic/DiagnosticParseKinds.td : #0:2
- clang/include/clang/Basic/DiagnosticSemaKinds.td : /\bdef\s+warn_contract_invalid_label_facet\b/, /\bdef\s+note_contract_invalid_label_facet\b/, /\bdef\s+err_contract_invalid_label\b/
- clang/include/clang/Basic/TokenKinds.def : #0:1, #2:1, #4:0
- clang/include/clang/Options/Options.td : @p3400
- clang/include/clang/Sema/Sema.h : #2
- clang/lib/Basic/IdentifierTable.cpp : #0:1
- clang/lib/Frontend/InitPreprocessor.cpp : #0:4
- clang/lib/Lex/Lexer.cpp : *
- clang/lib/Lex/LiteralSupport.cpp : *
- clang/lib/Parse/ParseDeclCXX.cpp : #1, #2
- clang/lib/Sema/SemaContract.cpp : @dropInaccessibleFacetCandidates, @evaluateFacetConstant, @FacetProbeArgs, @facetCallViable, @warnNearMissFacet, @callLabelMethod, @StringRef, @extractStringFromAPValue, @applyLabelFacets, @populateContractSemanticState
- clang/test/Contracts/Runnable/p3400-control-mixed.cpp : *
- clang/test/Contracts/Runnable/p3400-facet-chain-order.cpp : *
- clang/test/Contracts/Runnable/p3400-facet-ignore.cpp : *
- clang/test/Contracts/Runnable/p3400-facet-inaccessible.cpp : *
- clang/test/Contracts/Runnable/p3400-facet-inherited.cpp : *
- clang/test/Contracts/Runnable/p3400-facet-static-and-nonconst.cpp : *
- clang/test/Contracts/Runnable/p3400-label-constexpr.cpp : *
- clang/test/Contracts/Runnable/p3400-label-library.cpp : *
- clang/test/Contracts/p3400-combine-basic.cpp : *
- clang/test/Contracts/p3400-combine-lookup.cpp : *
- clang/test/Contracts/p3400-control-adl.cpp : *
- clang/test/Contracts/p3400-control-basic.cpp : *
- clang/test/Contracts/p3400-control-contexts.cpp : *
- clang/test/Contracts/p3400-control-expr.cpp : *
- clang/test/Contracts/p3400-control-function.cpp : *
- clang/test/Contracts/p3400-control-header.cpp : *
- clang/test/Contracts/p3400-control-scope.cpp : *
- clang/test/Contracts/p3400-facet-combined.cpp : *
- clang/test/Contracts/p3400-facet-empty-combine.cpp : *
- clang/test/Contracts/p3400-facet-near-miss.cpp : *
- clang/test/Contracts/p3400-facet-non-constexpr.cpp : *
- clang/test/Contracts/p3400-facet-permutations.cpp : *
- clang/test/Contracts/p3400-facet-review.cpp : *
- clang/test/Contracts/p3400-label-bad-type.cpp : *
- clang/test/Contracts/p3400-label-basic.cpp : *
- clang/test/Contracts/p3400-label-ftm.cpp : *
- clang/test/Contracts/p3400-label-late-parsed.cpp : *
- clang/test/Contracts/p3400-label-no-flag.cpp : *
- clang/test/Contracts/p3400-label-operators.cpp : *
- clang/test/Contracts/p3400-label-prvalue-template.cpp : *
- clang/test/Contracts/p3400-label-prvalue.cpp : *
- clang/test/Contracts/p3400-label-redecl.cpp : *
- clang/test/Contracts/p3400-label-rshift.cpp : *
- clang/test/Contracts/p3400-label-stateful-facet.cpp : *
- clang/test/Contracts/p3400-label-trailing-return.cpp : *
- libcxx/include/contracts : @labels, @empty_label_t, @empty_label, @_Tp, @_Ip, @_Np, @_Gp, @_Rhs, @_Ml, @review_t, @review, @fixed_message_label_t, @__gn_cmp, @__combined_label, @auto, /A const member cannot be filled by assignment/, /__gn_buf<__lc, __ml> __merge/, /__gn_buf<__rc, __ml> __merge/, /__gn_buf<__tc, __ml> __merge/
