---
id: 4000-p3098
subject: '[clang][contracts] postcondition captures'
depends: []
regenerates: []
fixes: []
---

## Rationale
P3098, "Contract Postcondition Captures": `post [c = x] (...)`, so a
postcondition can compare against a value taken *before* the body ran.

The feature is two-phase by construction and that is what most of the code
is about.  A capture's initializer is evaluated in the function prologue,
alongside the precondition checks; the predicate that reads it runs in the
epilogue.  Between those two points the body can do anything, including
throw, so the capture's storage has to outlive its own block scope and its
construction has to be guarded.

Two things worth a reviewer's attention:

**A capture whose construction throws is a `post_capture` violation, and
the guard is unconditional on purpose.**  `EmitPostconditionCaptureInit`
always wraps the construction in a catch-all, where the predicate path uses
`StmtCanThrow` to skip the try/catch when nothing can throw.  The comment
says why: `StmtCanThrow` special-cases `CallExpr` and
`CXXBindTemporaryExpr` but not a plain `CXXConstructExpr`, so it
false-negatives on the commonest case of all -- a constructor body that
throws -- and an unguarded exception would propagate out of the prologue.

**`observe` must skip the predicate, not fall through to it.**  When
construction fails under `observe` the violation is reported and execution
continues, but the capture does not exist; evaluating the predicate would
read it.  `CaptureInitFailed` is the per-capture flag that records this, and
`LoadPostconditionCaptureFailed` is what the epilogue checks.  There is no
re-check to fall back on here, unlike the predicate-throw path, because
construction either happened or did not.

**Constant evaluation is here too.**  `EvaluatePostconditionCaptures` binds
each capture in the constant-evaluation call frame at entry, with
`ScopeKind::Call` so the value survives the body's own block scopes
closing -- the mirror of what the codegen prologue does.  It lives inside
`HandleFunctionCall`, between the base facility's precondition call and its
return, so this commit claims those lines as declared cuts of the two
segments rather than the whole function.

**Two pieces of P3098 are not in this commit**, and a reviewer looking for
them should know where they went: the `PostconditionCapture` decl node is
in `1020-p2900-base-ast` with the rest of the AST, and the parser's token
handling for the capture list is in `1040-p2900-base-parse`.  Both are
slots that this commit fills, consistent with how those commits treat every
other paper.

## Compile gap
Needs `1020-p2900-base-ast` for the capture node and the `HasCaptures`
slot, `1040-p2900-base-parse` for the token handling, `1060-p2900-base-sema-core`
for the contract-specifier to attach captures to, `1100-p2900-base-codegen`
for the emission path, and `2400-p3595` for the semantic resolution the
capture path branches on.  All precede it.

None outward.

**Behaviour trap:** with the flag off, the capture syntax is diagnosed at
parse time, so the failure is loud.  The quiet failure is the other one --
capture *construction* order.  A capture initialized in the epilogue rather
than the prologue would read post-body state and every ordinary test would
still pass, because the values usually agree.  The tests that would catch
it are the ones that deliberately mutate the captured object in the body,
and they are the reason this entry has forty files.

## Contents
- clang/lib/AST/ExprConstant.cpp : @EvaluatePostconditionCaptures, #13:1, #16:0
- clang/include/clang/Basic/DiagnosticParseKinds.td : #0:0
- clang/include/clang/Basic/DiagnosticSemaKinds.td : /\bdef\s+err_postcondition_capture_by_reference\b/, /\bdef\s+err_postcondition_capture_default\b/, /\bdef\s+err_postcondition_capture_this\b/, /\bdef\s+err_postcondition_capture_not_parameter\b/, /\bdef\s+err_postcondition_capture_noncopyable\b/, /\bdef\s+err_postcondition_capture_nondestructible\b/
- clang/include/clang/Options/Options.td : @p3098
- clang/lib/CodeGen/CGContracts.cpp : @EmitPostconditionCaptureInit, /^CodeGenFunction::LoadPostconditionCaptureFailed/
- clang/lib/CodeGen/CodeGenFunction.cpp : #6
- clang/lib/Frontend/InitPreprocessor.cpp : #0:2
- clang/lib/Parse/ParseContracts.cpp : @ParsePostconditionCapt*
- clang/lib/Sema/SemaContract.cpp : @ActOnPostconditionCapture, @ActOnFinishPostconditionCaptures, @ContractCapturePair
- clang/test/Contracts/Runnable/p3098-basic-run.cpp : *
- clang/test/Contracts/Runnable/p3098-capture-init-template-comma.cpp : *
- clang/test/Contracts/Runnable/p3098-capture-member-late.cpp : *
- clang/test/Contracts/Runnable/p3098-capture-member-template.cpp : *
- clang/test/Contracts/Runnable/p3098-compute-semantic-capture.cpp : *
- clang/test/Contracts/Runnable/p3098-dtor.cpp : *
- clang/test/Contracts/Runnable/p3098-enforce-capture.cpp : *
- clang/test/Contracts/Runnable/p3098-except-body.cpp : *
- clang/test/Contracts/Runnable/p3098-except-dtor.cpp : *
- clang/test/Contracts/Runnable/p3098-except-init.cpp : *
- clang/test/Contracts/Runnable/p3098-except-predicate.cpp : *
- clang/test/Contracts/Runnable/p3098-group-ignore-capture.cpp : *
- clang/test/Contracts/Runnable/p3098-ignore-suppresses-capture.cpp : *
- clang/test/Contracts/Runnable/p3098-ignore.cpp : *
- clang/test/Contracts/Runnable/p3098-interleave.cpp : *
- clang/test/Contracts/Runnable/p3098-member-captures.cpp : *
- clang/test/Contracts/Runnable/p3098-noexcept.cpp : *
- clang/test/Contracts/Runnable/p3098-observe-capture.cpp : *
- clang/test/Contracts/Runnable/p3098-pack-run.cpp : *
- clang/test/Contracts/Runnable/p3098-template-member-capture.cpp : *
- clang/test/Contracts/p3098-capture-constexpr.cpp : *
- clang/test/Contracts/p3098-capture-member-template.cpp : *
- clang/test/Contracts/p3098-ftm.cpp : *
- clang/test/Contracts/p3098-name-lookup.cpp : *
- clang/test/Contracts/p3098-noflag.cpp : *
- clang/test/Contracts/p3098-noncopyable.cpp : *
- clang/test/Contracts/p3098-pack-basic.cpp : *
- clang/test/Contracts/p3098-pack-errors.cpp : *
- clang/test/Contracts/p3098-parse-errors.cpp : *
- clang/test/Contracts/p3098-redecl.cpp : *
- clang/test/Contracts/p3098-templates.cpp : *
- clang/test/Contracts/p3098-valid-parse.cpp : *
