---
id: 5000-p4299
subject: '[clang] contracts for C'
depends: []
regenerates: []
fixes: []
---

## Rationale
P4299, "Contracts for C": `_Pre`, `_Post` and `_ContractAssert` in the C
front end.

The keywords are added through `CONTRACTS_KEYWORD` in `TokenKinds.def`, and
the rest of the compiler-side work is one block in `ParseFunctionDeclarator`
that defers a C contract's tokens the same way C++ defers a member
function's.

**The interesting part of that block is the case it guards against.**  A
contract written on a *parameter's* own function declarator has nothing to
attach to: the parameter is not a function being defined, so the replay in
`ParseFunctionDefinition` never runs for it and the cached tokens would
reach `Declarator::clear()` unconsumed and trip its assertion.  It is
diagnosed and dropped instead.  The comment records the cross-compiler
position: GCC accepts and ignores the same construct, so ignoring it
matches -- the diagnostic is the only difference worth having.

C is **callee-side only** -- there is no caller-side checking in the C
front end on either compiler -- and P4299 is deliberately excluded from the
set of flags that imply `-fcontracts`, since it enables the C facility
rather than the C++ one.

The library side is `<stdcontracts.h>` and the `stdc_*` entry points, both
of which ride with `2200-libcontracts` because they are part of the C
runtime rather than of this front-end change.

## Compile gap
Needs `1040-p2900-base-parse` for the late-parsing machinery the C path
reuses, `1100-p2900-base-codegen` for emission, and `2200-libcontracts` for
the `stdc_*` entry points.  All precede it.

None outward.

**Behaviour trap:** with `-fcontracts-p4299` off, `_Pre` is not a keyword
and a C contract is a syntax error, which is loud.  The quiet failure is
the parameter-declarator case above -- before the guard it was an
assertion failure in a debug build and undefined behaviour in a release
one, which is the worst combination for something a user can write by
accident.

## Contents
- clang/include/clang/Basic/DiagnosticFrontendKinds.td : *
- clang/include/clang/Basic/TokenKinds.def : #2:2
- clang/include/clang/Options/Options.td : @p4299
- clang/lib/Parse/ParseDecl.cpp : #17
- clang/test/Contracts/C/assert-observe.c : *
- clang/test/Contracts/C/combined.c : *
- clang/test/Contracts/C/contract-on-non-function.c : *
- clang/test/Contracts/C/contracts-accessors.c : *
- clang/test/Contracts/C/contracts-config-location.c : *
- clang/test/Contracts/C/contracts-config-location.json : *
- clang/test/Contracts/C/contracts-handler-sig-nonconst.c : *
- clang/test/Contracts/C/contracts-handler-sig-nonptr.c : *
- clang/test/Contracts/C/contracts-handler-sig-noparam.c : *
- clang/test/Contracts/C/contracts-handler-sig-twoparam.c : *
- clang/test/Contracts/C/contracts-kind-macros.c : *
- clang/test/Contracts/C/contracts-post-order.c : *
- clang/test/Contracts/C/contracts-post-single-eval.c : *
- clang/test/Contracts/C/contracts-pre-ignore-illformed.c : *
- clang/test/Contracts/C/contracts-proto-leak.c : *
- clang/test/Contracts/C/contracts-stdcontracts-macros.c : *
- clang/test/Contracts/C/contracts-trigger-enforced.c : *
- clang/test/Contracts/C/contracts-trigger-observed.c : *
- clang/test/Contracts/C/contracts-trigger-quick.c : *
- clang/test/Contracts/C/handler-signature-bad.c : *
- clang/test/Contracts/C/implicit-link.c : *
- clang/test/Contracts/C/keywords-no-flag.c : *
- clang/test/Contracts/C/multi-pre.c : *
- clang/test/Contracts/C/param-declarator-contract.c : *
- clang/test/Contracts/C/post-no-result.c : *
- clang/test/Contracts/C/post-observe.c : *
- clang/test/Contracts/C/pre-enforce.c : *
- clang/test/Contracts/C/pre-ignore.c : *
- clang/test/Contracts/C/pre-observe.c : *
- clang/test/Contracts/C/stringize.c : *
- clang/test/Contracts/Runnable/p4299-c-is-terminating-noexcept.cpp : *
